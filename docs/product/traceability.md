# Requirement traceability

This matrix maps each locked product requirement to executable code and an exact release gate. Commands run from the repository root unless noted.

| Requirement | Implementation evidence | Exact gate |
| --- | --- | --- |
| Lightweight distributed backup and restore with Apple-level setup | Rust streaming engine; native SwiftUI/Compose clients; macOS app-owned node lifecycle; embedded authenticated console. | `./scripts/smoke.sh`; `swift test --package-path apps/apple`; universal Release archive inspection; live bundled macOS helper health check. |
| Memory-safe shared Rust backup, storage, verification, and networking engine | `covalent-core`, `covalent-protocol`, `covalent-node`, pinned QUIC transport, no unsafe core code. | `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`; `cargo test --locked --workspace --all-features`. |
| Native SwiftUI macOS and iOS apps | Shared authenticated model plus platform-native navigation, folder grants, menu bar, and bounded iOS background execution. | `swift test --package-path apps/apple`; generated `CovalentMac` and `CovalentIOS` Xcode builds; `apps/apple/Scripts/macos-ui-test.sh`; `apps/apple/Scripts/ios-ui-test.sh`. |
| Native Kotlin/Compose Android with restrained Material 3 and Pair/Backup/Restore toolbar | `apps/android` Compose application and three-action floating toolbar. | `./scripts/check-android.sh`; `./scripts/android-api37-device-test.sh` on exact `Covalent_API_37`. |
| Docker and Unraid for selected shares and optional boot backup | Rootless image, read-only source mounts, separate restore mounts, Unraid template opt-in boot mapping. | `docker build -f packaging/docker/Dockerfile -t covalent:integration .`; `./scripts/check-container-runtime.sh covalent:integration`; `./scripts/validate-unraid-template.sh`. |
| Easy secure pairing with LAN and Tailscale-aware discovery | Signed invitation/SAS confirmation, role grants, certificate pinning, bounded mDNS/Tailscale hints. | `cargo test --locked -p covalent-core pairing`; `cargo test --locked -p covalent-node transport`; `./scripts/docker-compose-e2e.sh covalent:integration`. |
| LAN discovery can be disabled | Persisted `lanDiscoveryEnabled`; disabled state does not start or browse mDNS. | `cargo test --locked -p covalent-node discovery::tests::disabled_lan_discovery_has_no_daemon_or_results`; settings import/export checks in `./scripts/smoke.sh`. |
| Selected-source backup and root-confined relative restore | Handle/root validation, signed immutable preview, symlink/traversal rejection; Android FD and Apple security-scoped archive bridges; empty-destination create-only streamed restore. | `./scripts/smoke.sh`; `apps/apple/Scripts/integration-test.sh`; `cargo test --locked -p covalent-node streamed_archive_bridge_never_requires_a_daemon_visible_android_path`; `cargo test --locked -p covalent-protocol adversarial_restore_path_fixture_is_rejected`. |
| User explicitly selects extra-copy devices; no automatic placement | `ReplicaIntent::explicit`, exact selected provider IDs in manifests and backup summaries. | `cargo test --locked -p covalent-protocol replicas_are_exactly_the_explicit_selection`; three-node Compose E2E checks two explicit replicas. |
| Authorized providers serve and verify chunks for multi-source recovery | Authenticated pinned QUIC providers, availability checks, corrupt-copy rejection and repair. | `cargo test --locked -p covalent-node mutually_paired_engines_backup_and_restore_over_pinned_quic`; `./scripts/docker-compose-e2e.sh covalent:integration`. |
| Safe settings transfer includes name/backups and excludes secrets | Strict `ExportedDeviceSettings`; private identity, token, bookmark, and provider credentials absent. | Rust/Swift golden fixture tests; `./scripts/smoke.sh`; `cargo test --locked -p covalent-core normal_settings_export_never_contains_identity_material`. |
| Real end-to-end functionality and production packaging | Real daemon integration from Swift, Android archive bridge against Rust, and temporary three-node container drill without hosted services. | `apps/apple/Scripts/integration-test.sh`; Rust archive E2E; `./scripts/docker-compose-e2e.sh covalent:integration`; headed platform gates. |
| Public `thekozugroup/Covalent` repository and direct-main delivery | `4a2fa5662632b9c14da9f8e4b0e4fc120b69044d` is pushed directly to public `main` by `thekozugroup <thekozugroup@gmail.com>`. | `gh repo view thekozugroup/Covalent`; verify remote `main`, author `thekozugroup <thekozugroup@gmail.com>`, required Tier 1 CI, and clean release worktree before publishing. |

## Shared contract fixtures

`cargo test --locked -p covalent-protocol --test contract_fixtures`, `swift test --package-path apps/apple`, and Android `:app:testDebugUnitTest` validate the committed version 1 configuration, manifest, pairing invitation, backup summary, transfer progress, node event, and API error contracts.

## Exact release-candidate evidence

The 2026-08-16 release-candidate pass is tied to `4a2fa5662632b9c14da9f8e4b0e4fc120b69044d`. `COVALENT_INCLUDE_IOS=1 ./scripts/check.sh`, `./scripts/android-api37-device-test.sh`, `./scripts/check-container-runtime.sh covalent:foundation`, `./scripts/docker-compose-e2e.sh covalent:foundation`, the unsigned macOS Release archive, and the iOS simulator UI flow passed. The ignored local evidence directory `artifacts/release-candidate-0.1.0-4a2fa56/` contains unsigned candidates, SHA-256 checksums, a Docker SPDX SBOM, Rust license inventory, benchmark output, and command logs. The macOS UI runner hung before executing cases in two bounded local attempts; this gate is not recorded as passed. Developer ID signing/notarization, live Unraid host drills, and CI-only image scanning/keyless signing remain release findings.
